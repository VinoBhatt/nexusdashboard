{
  "name": "api",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --config ../../wrangler.jsonc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "db:generate": "drizzle-kit generate",
    "db:migrate:local": "wrangler d1 migrations apply nexusdashboard-db --local --config ../../wrangler.jsonc",
    "db:migrate:remote": "wrangler d1 migrations apply nexusdashboard-db --remote --config ../../wrangler.jsonc",
    "db:seed:generate": "tsx src/db/seed.ts",
    "db:seed:local": "npm run db:seed:generate && wrangler d1 execute nexusdashboard-db --local --file=src/db/seed.sql --config ../../wrangler.jsonc"
  },
  "dependencies": {
    "hono": "^4.6.14",
    "drizzle-orm": "^0.45.2",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "@cloudflare/vitest-pool-workers": "^0.6.1",
    "drizzle-kit": "^0.31.0",
    "typescript": "~5.6.3",
    "vitest": "^2.1.8",
    "tsx": "^4.19.2",
    "wrangler": "^4.124.0"
  }
}
